export default function PartnerPage() {
  return (
    <div className="min-h-dvh flex items-center justify-center" style={{background:"var(--s0)"}}>
      <div className="text-center p-8">
        <div style={{fontSize:"32px",marginBottom:"12px"}}>🤝</div>
        <p style={{fontFamily:"var(--font-display)",fontSize:"20px",color:"var(--ink)",marginBottom:"8px"}}>Accountability Partner</p>
        <p style={{fontSize:"14px",color:"var(--ink3)"}}>Coming soon — invite a friend to build habits together.</p>
      </div>
    </div>
  )
}
